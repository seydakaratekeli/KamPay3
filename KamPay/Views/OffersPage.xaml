<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:KamPay.ViewModels"
             xmlns:models="clr-namespace:KamPay.Models"
             xmlns:converters="clr-namespace:KamPay.Converters"
             xmlns:ffimageloading="clr-namespace:FFImageLoading.Maui;assembly=FFImageLoading.Maui"
             xmlns:res="clr-namespace:KamPay.Services"
             x:Class="KamPay.Views.OffersPage"
             x:DataType="vm:OffersViewModel"
             Title="{Binding Source={x:Static res:LocalizationResourceManager.Instance}, Path=[OffersTitle]}"
             BackgroundColor="{StaticResource Gray100}">

    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:InvertedBoolConverter x:Key="InvertedBoolConverter" />
            <converters:IsAcceptedConverter x:Key="IsAcceptedConverter" />
            <converters:IsPendingConverter x:Key="IsPendingConverter" />
            <converters:ConfirmDonationButtonVisibilityConverter x:Key="ConfirmDonationButtonVisibilityConverter"/>
            <converters:SimulatePaymentButtonVisibilityConverter x:Key="SimulatePaymentButtonVisibilityConverter"/>
            
            <!-- 🔥 YENİ: Pazarlık Converter'ları -->
            <converters:IsSaleConverter x:Key="IsSaleConverter"/>
            <converters:IsTradeConverter x:Key="IsTradeConverter"/>
            <converters:AllTrueConverter x:Key="AllTrueConverter"/>
            <converters:IsCurrentUserConverter x:Key="IsCurrentUserConverter"/>
            <converters:ProductTypeToEmojiConverter x:Key="ProductTypeToEmojiConverter"/>
            <converters:IsNegotiatingConverter x:Key="IsNegotiatingConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid RowDefinitions="Auto,*">

        <!-- ÜST SEKMELER -->
        <Grid Grid.Row="0" ColumnDefinitions="*,*" Padding="15,10" ColumnSpacing="10" BackgroundColor="White">

            <!-- Gelen Teklifler -->
            <Button Grid.Column="0"
                    Text="{Binding Source={x:Static res:LocalizationResourceManager.Instance}, Path=[IncomingOffers]}"
                    Command="{Binding SelectIncomingCommand}"
                    HeightRequest="40"
                    CornerRadius="20"
                    FontSize="12"
                    FontAttributes="Bold"
                    BorderColor="{StaticResource Primary}"
                    BorderWidth="2">
                <Button.Triggers>
                    <DataTrigger TargetType="Button"
                                 Binding="{Binding IsIncomingSelected}"
                                 Value="True">
                        <Setter Property="BackgroundColor" Value="{StaticResource Primary}" />
                        <Setter Property="TextColor" Value="White" />
                    </DataTrigger>

                    <DataTrigger TargetType="Button"
                                 Binding="{Binding IsIncomingSelected}"
                                 Value="False">
                        <Setter Property="BackgroundColor" Value="White" />
                        <Setter Property="TextColor" Value="{StaticResource Primary}" />
                    </DataTrigger>
                </Button.Triggers>
            </Button>

            <!-- Giden Teklifler -->
            <Button Grid.Column="1"
                    Text="{Binding Source={x:Static res:LocalizationResourceManager.Instance}, Path=[OutgoingOffers]}"
                    Command="{Binding SelectOutgoingCommand}"
                    HeightRequest="40"
                    CornerRadius="20"
                    FontSize="12"
                    FontAttributes="Bold"
                    BorderColor="{StaticResource Primary}"
                    BorderWidth="2">
                <Button.Triggers>
                    <DataTrigger TargetType="Button"
                                 Binding="{Binding IsOutgoingSelected}"
                                 Value="True">
                        <Setter Property="BackgroundColor" Value="{StaticResource Primary}" />
                        <Setter Property="TextColor" Value="White" />
                    </DataTrigger>

                    <DataTrigger TargetType="Button"
                                 Binding="{Binding IsOutgoingSelected}"
                                 Value="False">
                        <Setter Property="BackgroundColor" Value="White" />
                        <Setter Property="TextColor" Value="{StaticResource Primary}" />
                    </DataTrigger>
                </Button.Triggers>
            </Button>
        </Grid>

        <!-- ANA İÇERİK -->
        <Grid Grid.Row="1">

            <!-- 🔥 1. SKELETON LOADER (İlk yükleme - En üstte) -->
            <ScrollView IsVisible="{Binding IsSkeletonVisible}">
                <VerticalStackLayout Padding="15" Spacing="12">
                    <!-- Skeleton Item 1 -->
                    <Frame CornerRadius="15" 
                           Padding="12" 
                           HasShadow="False" 
                           BackgroundColor="White"
                           BorderColor="{StaticResource Gray200}">
                        <Grid ColumnDefinitions="80,*" ColumnSpacing="10">
                            <BoxView Grid.Column="0" 
                                     WidthRequest="80" 
                                     HeightRequest="80" 
                                     CornerRadius="10"
                                     BackgroundColor="{StaticResource Gray200}"/>
                            <VerticalStackLayout Grid.Column="1" Spacing="8">
                                <BoxView HeightRequest="20" CornerRadius="4" BackgroundColor="{StaticResource Gray200}"/>
                                <BoxView HeightRequest="16" CornerRadius="4" BackgroundColor="{StaticResource Gray300}" WidthRequest="150" HorizontalOptions="Start"/>
                                <BoxView HeightRequest="16" CornerRadius="4" BackgroundColor="{StaticResource Gray300}" WidthRequest="200" HorizontalOptions="Start"/>
                            </VerticalStackLayout>
                        </Grid>
                    </Frame>

                    <!-- Skeleton Item 2 -->
                    <Frame CornerRadius="15" 
                           Padding="12" 
                           HasShadow="False" 
                           BackgroundColor="White"
                           BorderColor="{StaticResource Gray200}">
                        <Grid ColumnDefinitions="80,*" ColumnSpacing="10">
                            <BoxView Grid.Column="0" 
                                     WidthRequest="80" 
                                     HeightRequest="80" 
                                     CornerRadius="10"
                                     BackgroundColor="{StaticResource Gray200}"/>
                            <VerticalStackLayout Grid.Column="1" Spacing="8">
                                <BoxView HeightRequest="20" CornerRadius="4" BackgroundColor="{StaticResource Gray200}"/>
                                <BoxView HeightRequest="16" CornerRadius="4" BackgroundColor="{StaticResource Gray300}" WidthRequest="150" HorizontalOptions="Start"/>
                                <BoxView HeightRequest="16" CornerRadius="4" BackgroundColor="{StaticResource Gray300}" WidthRequest="200" HorizontalOptions="Start"/>
                            </VerticalStackLayout>
                        </Grid>
                    </Frame>

                    <!-- Skeleton Item 3 -->
                    <Frame CornerRadius="15" 
                           Padding="12" 
                           HasShadow="False" 
                           BackgroundColor="White"
                           BorderColor="{StaticResource Gray200}">
                        <Grid ColumnDefinitions="80,*" ColumnSpacing="10">
                            <BoxView Grid.Column="0" 
                                     WidthRequest="80" 
                                     HeightRequest="80" 
                                     CornerRadius="10"
                                     BackgroundColor="{StaticResource Gray200}"/>
                            <VerticalStackLayout Grid.Column="1" Spacing="8">
                                <BoxView HeightRequest="20" CornerRadius="4" BackgroundColor="{StaticResource Gray200}"/>
                                <BoxView HeightRequest="16" CornerRadius="4" BackgroundColor="{StaticResource Gray300}" WidthRequest="150" HorizontalOptions="Start"/>
                                <BoxView HeightRequest="16" CornerRadius="4" BackgroundColor="{StaticResource Gray300}" WidthRequest="200" HorizontalOptions="Start"/>
                            </VerticalStackLayout>
                        </Grid>
                    </Frame>
                </VerticalStackLayout>
            </ScrollView>

            <!-- 🔥 2. NORMAL İÇERİK (Skeleton kapandıktan sonra) -->
            <RefreshView Command="{Binding RefreshOffersCommand}"
                         IsRefreshing="{Binding IsRefreshing}"
                         RefreshColor="{StaticResource Primary}"
                         IsVisible="{Binding IsSkeletonVisible, Converter={StaticResource InvertedBoolConverter}}">

                <Grid>

                    <!-- GELEN TEKLİFLER -->
                    <CollectionView ItemsSource="{Binding IncomingOffers}"
                                    IsVisible="{Binding IsIncomingSelected}"
                                    Margin="10">

                        <CollectionView.EmptyView>
                            <VerticalStackLayout VerticalOptions="Center" 
                                                 HorizontalOptions="Center" 
                                                 Spacing="10" 
                                                 Padding="30"
                                                 IsVisible="{Binding HasIncomingOffers, Converter={StaticResource InvertedBoolConverter}}">
                                <Label Text="📥" FontSize="50" HorizontalTextAlignment="Center"/>
                                <Label Text="{Binding Source={x:Static res:LocalizationResourceManager.Instance}, Path=[NoIncomingOffers]}"
                                       TextColor="{StaticResource Gray600}"
                                       HorizontalTextAlignment="Center"
                                       FontSize="16"/>
                            </VerticalStackLayout>
                        </CollectionView.EmptyView>

                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="models:Transaction">
                                <Frame Margin="0,5"
                                       Padding="10"
                                       BorderColor="{StaticResource Gray200}"
                                       CornerRadius="15"
                                       HasShadow="True">

                                    <VerticalStackLayout Spacing="8">
                                        
                                        <Grid ColumnDefinitions="80,*" ColumnSpacing="10">

                                            <!-- Ürün Foto -->
                                            <ffimageloading:CachedImage Grid.Column="0"
                                                                        Source="{Binding ProductThumbnailUrl}"
                                                                        Aspect="AspectFill"
                                                                        HeightRequest="80"
                                                                        WidthRequest="80"
                                                                        LoadingPlaceholder="loading_image.png"
                                                                        ErrorPlaceholder="error_image.png">
                                                <ffimageloading:CachedImage.Clip>
                                                    <RoundRectangleGeometry CornerRadius="10" Rect="0,0,80,80"/>
                                                </ffimageloading:CachedImage.Clip>
                                            </ffimageloading:CachedImage>

                                            <!-- Sağ içerik -->
                                            <VerticalStackLayout Grid.Column="1" Spacing="5">

                                                <Grid ColumnDefinitions="*,Auto">
                                                    <Label Grid.Column="0"
                                                           Text="{Binding ProductTitle}"
                                                           TextColor="{StaticResource Gray900}"
                                                           FontAttributes="Bold"
                                                           FontSize="15"
                                                           LineBreakMode="TailTruncation"/>

                                                    <Label Grid.Column="1"
                                                           Text="{Binding StatusText}"
                                                           FontSize="11"
                                                           TextColor="{StaticResource Primary}"
                                                           FontAttributes="Bold"/>
                                                </Grid>

                                                <Label Text="{Binding BuyerName, StringFormat='Talep Eden: {0}'}"
                                                       FontSize="12"
                                                       TextColor="{StaticResource Gray600}"/>

                                                <Label Text="{Binding Message}"
                                                       FontSize="13"
                                                       MaxLines="2"
                                                       LineBreakMode="TailTruncation"
                                                       TextColor="{StaticResource Gray900}"/>
                                            </VerticalStackLayout>
                                        </Grid>

                                        <!-- 🔥 YENİ: Pazarlık Bilgi Paneli -->
                                        <Frame IsVisible="{Binding IsNegotiating}"
                                               BackgroundColor="#FF9800"
                                               Padding="10"
                                               Margin="0,5,0,0"
                                               CornerRadius="8"
                                               HasShadow="False">
                                            <VerticalStackLayout Spacing="5">
                                                <Label FontAttributes="Bold" TextColor="White" FontSize="12">
                                                    <Label.FormattedText>
                                                        <FormattedString>
                                                            <Span Text="{Binding Type, Converter={StaticResource ProductTypeToEmojiConverter}}"/>
                                                            <Span Text=" Pazarlık Devam Ediyor"/>
                                                        </FormattedString>
                                                    </Label.FormattedText>
                                                </Label>
                                                <Label Text="{Binding NegotiationStatusText}"
                                                       TextColor="White"
                                                       FontSize="11"/>
                                            </VerticalStackLayout>
                                        </Frame>

                                        <BoxView HeightRequest="1" Color="{StaticResource Gray200}" Margin="0,5"/>

                                        <!-- Butonlar -->
                                        <Grid ColumnDefinitions="Auto,*">

                                            <Label Text="{Binding Price, StringFormat='{0} ₺'}"
                                                   TextColor="{StaticResource Primary}"
                                                   FontAttributes="Bold"
                                                   VerticalOptions="Center"/>

                                            <HorizontalStackLayout Grid.Column="1" HorizontalOptions="End" Spacing="5">

                                                <!-- 💬 Mesaj Butonu -->
                                                <Button Text="💬"
                                                        Command="{Binding Source={RelativeSource AncestorType={x:Type vm:OffersViewModel}}, Path=MessagePartnerCommand}"
                                                        CommandParameter="{Binding .}"
                                                        BackgroundColor="{StaticResource Secondary}"
                                                        TextColor="White"
                                                        WidthRequest="40"
                                                        HeightRequest="30"
                                                        CornerRadius="5"
                                                        FontSize="14"
                                                        Padding="0"/>

                                                <!-- 💰 Karşı Teklif (SATIŞ + Satıcı + Pending) -->
                                                <Button Text="💰"
                                                        Command="{Binding Source={RelativeSource AncestorType={x:Type vm:OffersViewModel}}, Path=SendCounterOfferCommand}"
                                                        CommandParameter="{Binding .}"
                                                        BackgroundColor="{StaticResource Tertiary}"
                                                        TextColor="White"
                                                        WidthRequest="40"
                                                        HeightRequest="30"
                                                        CornerRadius="5"
                                                        FontSize="14"
                                                        Padding="0">
                                                    <Button.IsVisible>
                                                        <MultiBinding Converter="{StaticResource AllTrueConverter}">
                                                            <Binding Path="Type" Converter="{StaticResource IsSaleConverter}"/>
                                                            <Binding Path="Status" Converter="{StaticResource IsPendingConverter}"/>
                                                            <Binding Path="SellerId" Converter="{StaticResource IsCurrentUserConverter}"/>
                                                        </MultiBinding>
                                                    </Button.IsVisible>
                                                </Button>

                                                <!-- 💰 Karşı Nakit (TAKAS + Sahip + Pending) -->
                                                <Button Text="💰"
                                                        Command="{Binding Source={RelativeSource AncestorType={x:Type vm:OffersViewModel}}, Path=SendCounterOfferCommand}"
                                                        CommandParameter="{Binding .}"
                                                        BackgroundColor="{StaticResource Tertiary}"
                                                        TextColor="White"
                                                        WidthRequest="40"
                                                        HeightRequest="30"
                                                        CornerRadius="5"
                                                        FontSize="14"
                                                        Padding="0">
                                                    <Button.IsVisible>
                                                        <MultiBinding Converter="{StaticResource AllTrueConverter}">
                                                            <Binding Path="Type" Converter="{StaticResource IsTradeConverter}"/>
                                                            <Binding Path="Status" Converter="{StaticResource IsPendingConverter}"/>
                                                            <Binding Path="SellerId" Converter="{StaticResource IsCurrentUserConverter}"/>
                                                        </MultiBinding>
                                                    </Button.IsVisible>
                                                </Button>

                                                <!-- ✓ Kabul (Pazarlık devam ederken) -->
                                                <Button Text="✓"
                                                        Command="{Binding Source={RelativeSource AncestorType={x:Type vm:OffersViewModel}}, Path=AcceptNegotiatedPriceCommand}"
                                                        CommandParameter="{Binding .}"
                                                        IsVisible="{Binding IsNegotiating}"
                                                        BackgroundColor="#4CAF50"
                                                        TextColor="White"
                                                        WidthRequest="40"
                                                        HeightRequest="30"
                                                        CornerRadius="5"
                                                        FontSize="14"
                                                        Padding="0"/>

                                                <Button Text="{Binding Source={x:Static res:LocalizationResourceManager.Instance}, Path=[Reject]}"
                                                        Command="{Binding Source={RelativeSource AncestorType={x:Type vm:OffersViewModel}}, Path=RejectOfferCommand}"
                                                        CommandParameter="{Binding .}"
                                                        BackgroundColor="{StaticResource Tertiary}"
                                                        TextColor="White"
                                                        HeightRequest="30"
                                                        CornerRadius="5"
                                                        FontSize="11"
                                                        IsVisible="{Binding Status, Converter={StaticResource IsPendingConverter}}"/>

                                                <Button Text="{Binding Source={x:Static res:LocalizationResourceManager.Instance}, Path=[Accept]}"
                                                        Command="{Binding Source={RelativeSource AncestorType={x:Type vm:OffersViewModel}}, Path=AcceptOfferCommand}"
                                                        CommandParameter="{Binding .}"
                                                        BackgroundColor="{StaticResource Primary}"
                                                        TextColor="White"
                                                        HeightRequest="30"
                                                        CornerRadius="5"
                                                        FontSize="11"
                                                        IsVisible="{Binding Status, Converter={StaticResource IsPendingConverter}}"/>

                                                <Button Text="{Binding Source={x:Static res:LocalizationResourceManager.Instance}, Path=[DeliveryQR]}"
                                                        Command="{Binding Source={RelativeSource AncestorType={x:Type vm:OffersViewModel}}, Path=ManageDeliveryCommand}"
                                                        CommandParameter="{Binding .}"
                                                        BackgroundColor="{StaticResource Secondary}"
                                                        TextColor="White"
                                                        HeightRequest="30"
                                                        CornerRadius="5"
                                                        FontSize="11"
                                                        IsVisible="{Binding CanManageDelivery}"/>

                                            </HorizontalStackLayout>
                                        </Grid>

                                    </VerticalStackLayout>

                                </Frame>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>

                    </CollectionView>

                    <!-- GİDEN TEKLİFLER -->
                    <CollectionView ItemsSource="{Binding OutgoingOffers}"
                                    IsVisible="{Binding IsOutgoingSelected}"
                                    Margin="10">

                        <CollectionView.EmptyView>
                            <VerticalStackLayout VerticalOptions="Center" 
                                                 HorizontalOptions="Center" 
                                                 Spacing="10" 
                                                 Padding="30"
                                                 IsVisible="{Binding HasOutgoingOffers, Converter={StaticResource InvertedBoolConverter}}">
                                <Label Text="📤" FontSize="50" HorizontalTextAlignment="Center"/>
                                <Label Text="{Binding Source={x:Static res:LocalizationResourceManager.Instance}, Path=[NoOutgoingOffers]}"
                                       TextColor="{StaticResource Gray600}"
                                       HorizontalTextAlignment="Center"
                                       FontSize="16"/>
                            </VerticalStackLayout>
                        </CollectionView.EmptyView>

                        <CollectionView.ItemTemplate>
                            <DataTemplate x:DataType="models:Transaction">
                                <Frame Margin="0,5"
                                       Padding="10"
                                       BorderColor="{StaticResource Gray200}"
                                       CornerRadius="15"
                                       HasShadow="True"
                                       BackgroundColor="#FAFAFA">

                                    <VerticalStackLayout Spacing="8">
                                        
                                        <Grid ColumnDefinitions="80,*" ColumnSpacing="10">

                                            <ffimageloading:CachedImage Grid.Column="0"
                                                                        Source="{Binding ProductThumbnailUrl}"
                                                                        Aspect="AspectFill"
                                                                        HeightRequest="80"
                                                                        WidthRequest="80"
                                                                        LoadingPlaceholder="loading_image.png"
                                                                        ErrorPlaceholder="error_image.png">
                                                <ffimageloading:CachedImage.Clip>
                                                    <RoundRectangleGeometry CornerRadius="10" Rect="0,0,80,80"/>
                                                </ffimageloading:CachedImage.Clip>
                                            </ffimageloading:CachedImage>

                                            <VerticalStackLayout Grid.Column="1" Spacing="5">

                                                <Grid ColumnDefinitions="*,Auto">
                                                    <Label Grid.Column="0"
                                                           Text="{Binding ProductTitle}"
                                                           FontAttributes="Bold"
                                                           FontSize="15"
                                                           TextColor="{StaticResource Gray900}"
                                                           LineBreakMode="TailTruncation" />

                                                    <Label Grid.Column="1"
                                                           Text="{Binding StatusText}"
                                                           FontSize="11"
                                                           TextColor="{StaticResource Secondary}"
                                                           FontAttributes="Bold"/>
                                                </Grid>

                                                <Label Text="{Binding SellerName, StringFormat='Satıcı: {0}'}"
                                                       FontSize="12"
                                                       TextColor="{StaticResource Gray600}"/>

                                                <Label Text="{Binding Message}"
                                                       FontSize="13"
                                                       MaxLines="2"
                                                       LineBreakMode="TailTruncation"
                                                       TextColor="{StaticResource Gray900}"/>

                                            </VerticalStackLayout>
                                        </Grid>

                                        <!-- 🔥 YENİ: Pazarlık Bilgi Paneli -->
                                        <Frame IsVisible="{Binding IsNegotiating}"
                                               BackgroundColor="#FF9800"
                                               Padding="10"
                                               Margin="0,5,0,0"
                                               CornerRadius="8"
                                               HasShadow="False">
                                            <VerticalStackLayout Spacing="5">
                                                <Label FontAttributes="Bold" TextColor="White" FontSize="12">
                                                    <Label.FormattedText>
                                                        <FormattedString>
                                                            <Span Text="{Binding Type, Converter={StaticResource ProductTypeToEmojiConverter}}"/>
                                                            <Span Text=" Pazarlık Devam Ediyor"/>
                                                        </FormattedString>
                                                    </Label.FormattedText>
                                                </Label>
                                                <Label Text="{Binding NegotiationStatusText}"
                                                       TextColor="White"
                                                       FontSize="11"/>
                                            </VerticalStackLayout>
                                        </Frame>

                                        <BoxView HeightRequest="1" Color="{StaticResource Gray200}" Margin="0,5"/>

                                        <Grid ColumnDefinitions="Auto,*">

                                            <Label Text="{Binding Price, StringFormat='{0} ₺'}"
                                                   TextColor="{StaticResource Primary}"
                                                   FontAttributes="Bold"
                                                   VerticalOptions="Center"/>

                                            <HorizontalStackLayout Grid.Column="1" HorizontalOptions="End" Spacing="5">

                                                <!-- 💬 Mesaj Butonu -->
                                                <Button Text="💬"
                                                        Command="{Binding Source={RelativeSource AncestorType={x:Type vm:OffersViewModel}}, Path=MessagePartnerCommand}"
                                                        CommandParameter="{Binding .}"
                                                        BackgroundColor="{StaticResource Secondary}"
                                                        TextColor="White"
                                                        WidthRequest="40"
                                                        HeightRequest="30"
                                                        CornerRadius="5"
                                                        FontSize="14"
                                                        Padding="0"/>

                                                <!-- 💰 Fiyat Teklifi (SATIŞ + Alıcı + Pending) -->
                                                <Button Text="💰"
                                                        Command="{Binding Source={RelativeSource AncestorType={x:Type vm:OffersViewModel}}, Path=ProposePriceCommand}"
                                                        CommandParameter="{Binding .}"
                                                        BackgroundColor="{StaticResource Tertiary}"
                                                        TextColor="White"
                                                        WidthRequest="40"
                                                        HeightRequest="30"
                                                        CornerRadius="5"
                                                        FontSize="14"
                                                        Padding="0">
                                                    <Button.IsVisible>
                                                        <MultiBinding Converter="{StaticResource AllTrueConverter}">
                                                            <Binding Path="Type" Converter="{StaticResource IsSaleConverter}"/>
                                                            <Binding Path="Status" Converter="{StaticResource IsPendingConverter}"/>
                                                            <Binding Path="BuyerId" Converter="{StaticResource IsCurrentUserConverter}"/>
                                                        </MultiBinding>
                                                    </Button.IsVisible>
                                                </Button>

                                                <!-- 💰 Ek Nakit (TAKAS + Talep Eden + Pending) -->
                                                <Button Text="💰"
                                                        Command="{Binding Source={RelativeSource AncestorType={x:Type vm:OffersViewModel}}, Path=ProposePriceCommand}"
                                                        CommandParameter="{Binding .}"
                                                        BackgroundColor="{StaticResource Tertiary}"
                                                        TextColor="White"
                                                        WidthRequest="40"
                                                        HeightRequest="30"
                                                        CornerRadius="5"
                                                        FontSize="14"
                                                        Padding="0">
                                                    <Button.IsVisible>
                                                        <MultiBinding Converter="{StaticResource AllTrueConverter}">
                                                            <Binding Path="Type" Converter="{StaticResource IsTradeConverter}"/>
                                                            <Binding Path="Status" Converter="{StaticResource IsPendingConverter}"/>
                                                            <Binding Path="BuyerId" Converter="{StaticResource IsCurrentUserConverter}"/>
                                                        </MultiBinding>
                                                    </Button.IsVisible>
                                                </Button>

                                                <!-- ✓ Kabul (Pazarlık devam ederken) -->
                                                <Button Text="✓"
                                                        Command="{Binding Source={RelativeSource AncestorType={x:Type vm:OffersViewModel}}, Path=AcceptNegotiatedPriceCommand}"
                                                        CommandParameter="{Binding .}"
                                                        IsVisible="{Binding IsNegotiating}"
                                                        BackgroundColor="#4CAF50"
                                                        TextColor="White"
                                                        WidthRequest="40"
                                                        HeightRequest="30"
                                                        CornerRadius="5"
                                                        FontSize="14"
                                                        Padding="0"/>

                                                <Button Text="{Binding Source={x:Static res:LocalizationResourceManager.Instance}, Path=[MakePayment]}"
                                                        Command="{Binding Source={RelativeSource AncestorType={x:Type vm:OffersViewModel}}, Path=CompletePaymentCommand}"
                                                        CommandParameter="{Binding .}"
                                                        BackgroundColor="{StaticResource Primary}"
                                                        TextColor="White"
                                                        HeightRequest="30"
                                                        CornerRadius="5"
                                                        FontSize="11"
                                                        IsVisible="{Binding Path=., Converter={StaticResource SimulatePaymentButtonVisibilityConverter}}" />

                                                <Button Text="{Binding Source={x:Static res:LocalizationResourceManager.Instance}, Path=[ConfirmReceived]}"
                                                        Command="{Binding Source={RelativeSource AncestorType={x:Type vm:OffersViewModel}}, Path=ConfirmDonationReceivedCommand}"
                                                        CommandParameter="{Binding .}"
                                                        BackgroundColor="#4CAF50"
                                                        TextColor="White"
                                                        HeightRequest="30"
                                                        CornerRadius="5"
                                                        FontSize="11"
                                                        IsVisible="{Binding Path=., Converter={StaticResource ConfirmDonationButtonVisibilityConverter}}" />

                                                <Button Text="{Binding Source={x:Static res:LocalizationResourceManager.Instance}, Path=[DeliveryQR]}"
                                                        Command="{Binding Source={RelativeSource AncestorType={x:Type vm:OffersViewModel}}, Path=ManageDeliveryCommand}"
                                                        CommandParameter="{Binding .}"
                                                        BackgroundColor="{StaticResource Secondary}"
                                                        TextColor="White"
                                                        HeightRequest="30"
                                                        CornerRadius="5"
                                                        FontSize="11"
                                                        IsVisible="{Binding CanManageDelivery}" />

                                            </HorizontalStackLayout>
                                        </Grid>

                                    </VerticalStackLayout>

                                </Frame>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>

                    </CollectionView>

                </Grid>
            </RefreshView>

            <!-- 🔥 3. LOADING INDICATOR (Tüm ekranın ortasında - En üstte) -->
            <Grid IsVisible="{Binding IsLoading}" BackgroundColor="Transparent">
                <VerticalStackLayout VerticalOptions="Center" HorizontalOptions="Center" Spacing="15">
                    <ActivityIndicator IsRunning="True"
                                       Color="{StaticResource Primary}"
                                       WidthRequest="50"
                                       HeightRequest="50"/>
                    <Label Text="{Binding Source={x:Static res:LocalizationResourceManager.Instance}, Path=[OffersLoading]}"
                           TextColor="{StaticResource Gray600}"
                           FontSize="14"
                           HorizontalTextAlignment="Center"/>
                </VerticalStackLayout>
            </Grid>

        </Grid>
    </Grid>
</ContentPage>
