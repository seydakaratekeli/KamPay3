<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:KamPay.ViewModels"
             xmlns:models="clr-namespace:KamPay.Models"
             xmlns:converters="clr-namespace:KamPay.Converters"
             xmlns:ffimageloading="clr-namespace:FFImageLoading.Maui;assembly=FFImageLoading.Maui"
             x:Class="KamPay.Views.ServiceSharingPage"
             x:DataType="vm:ServiceSharingViewModel"
             BackgroundColor="{StaticResource Gray100}">

    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:InverseBoolConverter x:Key="InverseBoolConverter" />
            <converters:ServiceCategoryToTextConverter x:Key="ServiceCategoryToTextConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid>

        <!-- ============================================== -->
        <!--  🔥  SKELETON LOADER (IsLoading == true)        -->
        <!-- ============================================== -->
        <ScrollView
            IsVisible="{Binding IsLoading}"
            BackgroundColor="{StaticResource Gray100}" Padding="10">

            <VerticalStackLayout Spacing="10">

                <Frame CornerRadius="15" HeightRequest="150"
                       BackgroundColor="{StaticResource Gray200}" Opacity="0.6" />

                <Frame CornerRadius="15" HeightRequest="150"
                       BackgroundColor="{StaticResource Gray200}" Opacity="0.6" />

                <Frame CornerRadius="15" HeightRequest="150"
                       BackgroundColor="{StaticResource Gray200}" Opacity="0.6" />

            </VerticalStackLayout>

        </ScrollView>


        <!-- ============================================== -->
        <!--  🔥  REFRESH + ACTUAL LIST                      -->
        <!-- ============================================== -->
        <RefreshView
            Command="{Binding RefreshServicesCommand}"
            IsRefreshing="{Binding IsRefreshing}"
            IsVisible="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}"
            RefreshColor="{StaticResource Primary}">

            <CollectionView ItemsSource="{Binding Services}"
                            Margin="10,0"
                            SelectionMode="None">

                <!-- ===== HEADER ===== -->
                <CollectionView.Header>
                    <Label Text="Mevcut Hizmetler"
                           FontSize="20"
                           FontAttributes="Bold"
                           TextColor="{StaticResource Primary}"
                           Margin="10,15"/>
                </CollectionView.Header>

                <!-- ===== EMPTY VIEW ===== -->
                <CollectionView.EmptyView>
                    <VerticalStackLayout Padding="40"
                                         VerticalOptions="Center"
                                         HorizontalOptions="Center"
                                         Spacing="15">

                        <Label Text="💼"
                               FontSize="60"
                               Opacity="0.3"
                               HorizontalTextAlignment="Center"/>

                        <Label Text="Henüz hizmet paylaşılmamış."
                               FontSize="17"
                               FontAttributes="Bold"
                               HorizontalTextAlignment="Center"
                               TextColor="{StaticResource Gray600}"/>

                        <Label Text="İlk hizmeti siz ekleyin!"
                               FontSize="14"
                               HorizontalTextAlignment="Center"
                               TextColor="{StaticResource Primary}"/>
                    </VerticalStackLayout>
                </CollectionView.EmptyView>

                <!-- ============================================== -->
                <!--  🔥 HIZMET ITEM TEMPLATE                        -->
                <!-- ============================================== -->
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:ServiceOffer">

                        <Frame Margin="0,8"
                               Padding="15"
                               BorderColor="{StaticResource Gray200}"
                               CornerRadius="15"
                               BackgroundColor="White"
                               HasShadow="True">

                            <VerticalStackLayout Spacing="10">

                                <!-- Title + Category -->
                                <Grid ColumnDefinitions="*,Auto">
                                    <Label Text="{Binding Title}"
                                           FontAttributes="Bold"
                                           FontSize="18"
                                           TextColor="{StaticResource Gray900}"
                                           LineBreakMode="TailTruncation"/>

                                    <Border Grid.Column="1"
                                            BackgroundColor="{StaticResource Gray200}"
                                            Padding="10,5"
                                            StrokeShape="RoundRectangle 8">
                                        <Label Text="{Binding Category, Converter={StaticResource ServiceCategoryToTextConverter}}"
                                               FontSize="12"
                                               TextColor="{StaticResource Primary}"
                                               FontAttributes="Bold"/>
                                    </Border>
                                </Grid>

                                <!-- Description -->
                                <Label Text="{Binding Description}"
                                       TextColor="{StaticResource Gray600}"
                                       LineBreakMode="WordWrap"
                                       FontSize="14"/>

                                <BoxView HeightRequest="1"
                                         Color="{StaticResource Gray200}"
                                         Margin="0,5"/>

                                <!-- PRICE + PROVIDER + REQUEST BUTTON -->
                                <Grid ColumnDefinitions="*,Auto" RowDefinitions="Auto">

                                    <!-- PROVIDER -->
                                    <HorizontalStackLayout Grid.Column="0" Spacing="10" VerticalOptions="Center">
                                        <ffimageloading:CachedImage
                                            Source="{Binding ProviderPhotoUrl}"
                                            Aspect="AspectFill"
                                            HeightRequest="40"
                                            WidthRequest="40"
                                            LoadingPlaceholder="person_icon.svg"
                                            ErrorPlaceholder="person_icon.svg">
                                            <ffimageloading:CachedImage.Clip>
                                                <EllipseGeometry Center="20,20" RadiusX="20" RadiusY="20"/>
                                            </ffimageloading:CachedImage.Clip>
                                        </ffimageloading:CachedImage>

                                        <VerticalStackLayout>
                                            <Label Text="{Binding ProviderName}"
                                                   FontAttributes="Bold"
                                                   FontSize="14"
                                                   TextColor="{StaticResource Gray900}"/>

                                            <Label Text="{Binding CreatedAt, Converter={StaticResource DateTimeToTimeAgoConverter}}"
                                                   FontSize="11"
                                                   TextColor="{StaticResource Gray500}"/>
                                        </VerticalStackLayout>
                                    </HorizontalStackLayout>

                                    <!-- PRICE + BUTTON -->
                                    <HorizontalStackLayout Grid.Column="1" Spacing="15" VerticalOptions="Center">
                                        <VerticalStackLayout Spacing="2">
                                            <Label Text="{Binding Price, StringFormat='{0} ₺'}"
                                                   FontAttributes="Bold"
                                                   FontSize="16"
                                                   TextColor="{StaticResource Primary}"/>

                                            <Label Text="{Binding TimeCredits, StringFormat='⏱ {0} Saat'}"
                                                   FontSize="12"
                                                   TextColor="{StaticResource Secondary}"/>
                                        </VerticalStackLayout>

                                        <Button Text="Talep Et"
                                                Command="{Binding Source={RelativeSource AncestorType={x:Type vm:ServiceSharingViewModel}}, Path=RequestServiceCommand}"
                                                CommandParameter="{Binding .}"
                                                BackgroundColor="{StaticResource Primary}"
                                                Padding="15,0"
                                                TextColor="White"
                                                CornerRadius="10"
                                                HeightRequest="40"
                                                FontAttributes="Bold"/>
                                    </HorizontalStackLayout>

                                </Grid>

                            </VerticalStackLayout>
                        </Frame>

                    </DataTemplate>
                </CollectionView.ItemTemplate>

            </CollectionView>
        </RefreshView>


        <!-- ============================================== -->
        <!--  🔥 FORM - POST POPUP                          -->
        <!-- ============================================== -->
        <Grid IsVisible="{Binding IsPostFormVisible}"
              BackgroundColor="#80000000"
              ZIndex="99">

            <Grid.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding ClosePostFormCommand}" />
            </Grid.GestureRecognizers>

            <Border BackgroundColor="White"
                    VerticalOptions="End"
                    StrokeShape="RoundRectangle 20"
                    Padding="20">

                <VerticalStackLayout Spacing="15">

                    <Grid ColumnDefinitions="*,Auto">
                        <Label Text="Yeni Hizmet Paylaş"
                               FontSize="20"
                               FontAttributes="Bold"/>

                        <Button Text="✕"
                                Command="{Binding ClosePostFormCommand}"
                                BackgroundColor="Transparent"
                                FontSize="22"
                                Padding="0"/>
                    </Grid>

                    <BoxView HeightRequest="1" Color="{StaticResource Gray300}" />

                    <Entry Placeholder="Hizmet Başlığı"
                           Text="{Binding ServiceTitle}"/>

                    <Border Stroke="{StaticResource Gray300}"
                            StrokeShape="RoundRectangle 10"
                            Padding="10">
                        <Editor Placeholder="Hizmetin Detayları..."
                                Text="{Binding ServiceDescription}"
                                HeightRequest="80"/>
                    </Border>

                    <Label Text="Kategori" FontSize="12" TextColor="{StaticResource Gray600}"/>

                    <Picker Title="Kategori"
                            ItemsSource="{Binding Categories}"
                            SelectedItem="{Binding SelectedCategory}"
                            ItemDisplayBinding="{Binding ., Converter={StaticResource ServiceCategoryToTextConverter}}"/>

                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10">

                        <!-- Price -->
                        <VerticalStackLayout>
                            <Label Text="Fiyat (₺)" FontSize="12" TextColor="{StaticResource Gray600}" />
                            <Entry Text="{Binding ServicePrice}" Keyboard="Numeric" />
                        </VerticalStackLayout>

                        <!-- Time Credits -->
                        <VerticalStackLayout>
                            <Label Text="Süre (Saat)" FontSize="12" TextColor="{StaticResource Gray600}" />
                            <Stepper Value="{Binding TimeCredits}" Minimum="1" Maximum="10"/>
                        </VerticalStackLayout>

                    </Grid>

                    <Button Text="Hizmeti Paylaş"
                            Command="{Binding CreateServiceCommand}"
                            BackgroundColor="{StaticResource Primary}"
                            TextColor="White"
                            CornerRadius="10"
                            HeightRequest="50"
                            FontAttributes="Bold"/>
                </VerticalStackLayout>

            </Border>

        </Grid>


        <!-- ============================================== -->
        <!--  🔥 POSTING OVERLAY                           -->
        <!-- ============================================== -->
        <Grid IsVisible="{Binding IsPosting}"
              BackgroundColor="#80000088"
              ZIndex="100">
            <VerticalStackLayout VerticalOptions="Center"
                                 HorizontalOptions="Center"
                                 Spacing="10">
                <ActivityIndicator IsRunning="True" Color="White" WidthRequest="50"/>
                <Label Text="İşlem yapılıyor..." TextColor="White"/>
            </VerticalStackLayout>
        </Grid>


        <!-- ============================================== -->
        <!--  🔥 FAB BUTTON (+)                            -->
        <!-- ============================================== -->
        <Button Text="+"
                Command="{Binding OpenPostFormCommand}"
                BackgroundColor="{StaticResource Primary}"
                TextColor="White"
                FontSize="32"
                CornerRadius="30"
                WidthRequest="60"
                HeightRequest="60"
                Padding="0"
                VerticalOptions="End"
                HorizontalOptions="End"
                Margin="25"
                ZIndex="10">
            <Button.Shadow>
                <Shadow Brush="Black" Offset="0,5" Opacity="0.3" Radius="8"/>
            </Button.Shadow>
        </Button>

    </Grid>

</ContentPage>
