<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:res="clr-namespace:KamPay.Services"
    x:Class="KamPay.Views.LoginPage"
    Title="{Binding Source={x:Static res:LocalizationResourceManager.Instance}, Path=[LoginTitle]}">

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="12">

            <!-- 💡 DEMO KULLANICI BİLGİLERİ -->
            <Label Text="{Binding Source={x:Static res:LocalizationResourceManager.Instance}, Path=[DemoUserInfo]}"
                   FontAttributes="Bold"
                   TextColor="#333"
                   FontSize="14"
                   HorizontalOptions="Center" />

            <HorizontalStackLayout Spacing="10">
                <!-- Demo Kullanıcı 1 -->
                <Frame Padding="10"
                       CornerRadius="10"
                       BorderColor="#E0E0E0"
                       BackgroundColor="#F5F5F5"
                       WidthRequest="160">
                    <VerticalStackLayout Spacing="5">
                        <Label Text="👤 Kullanıcı 1"
                               FontAttributes="Bold"
                               TextColor="#333"
                               FontSize="14" />
                        <Label Text="E-posta: Mehmetakar@bartin.edu.tr"
                               FontSize="13"
                               TextColor="#555" />
                        <Label Text="Şifre: Mehmet1234"
                               FontSize="13"
                               TextColor="#555" />
                    </VerticalStackLayout>
                </Frame>

                <!-- Demo Kullanıcı 2 -->
                <Frame Padding="10"
                       CornerRadius="10"
                       BorderColor="#E0E0E0"
                       BackgroundColor="#F5F5F5"
                       WidthRequest="160">
                    <VerticalStackLayout Spacing="5">
                        <Label Text="👤 Kullanıcı 2"
                               FontAttributes="Bold"
                               TextColor="#333"
                               FontSize="14" />
                        <Label Text="E-posta: Elifguler@bartin.edu.tr"
                               FontSize="13"
                               TextColor="#555" />
                        <Label Text="Şifre: Elif1234"
                               FontSize="13"
                               TextColor="#555" />
                    </VerticalStackLayout>
                </Frame>
            </HorizontalStackLayout>

            <Label Text="{Binding Source={x:Static res:LocalizationResourceManager.Instance}, Path=[LoginTitle]}" FontSize="24" HorizontalOptions="Center" />

            <Entry Placeholder="{Binding Source={x:Static res:LocalizationResourceManager.Instance}, Path=[Email]}" Text="{Binding Email}" Keyboard="Email" />
            <Entry Placeholder="{Binding Source={x:Static res:LocalizationResourceManager.Instance}, Path=[Password]}" Text="{Binding Password}" IsPassword="True" />

            <HorizontalStackLayout VerticalOptions="Center">
                <Switch IsToggled="{Binding RememberMe}" />
                <Label Text="{Binding Source={x:Static res:LocalizationResourceManager.Instance}, Path=[RememberMe]}" VerticalOptions="Center" />
            </HorizontalStackLayout>

            <Label Text="{Binding ErrorMessage}" 
                   IsVisible="{Binding ErrorMessage}" 
                   TextColor="Red" />

            <Button Text="{Binding Source={x:Static res:LocalizationResourceManager.Instance}, Path=[LoginTitle]}" Command="{Binding LoginCommand}" />
            <ActivityIndicator IsRunning="{Binding IsLoading}" IsVisible="{Binding IsLoading}" />

            <Button Text="{Binding Source={x:Static res:LocalizationResourceManager.Instance}, Path=[NoAccountRegister]}" 
                    Command="{Binding GoToRegisterCommand}" 
                    BackgroundColor="Transparent" 
                    TextColor="Blue" />

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
